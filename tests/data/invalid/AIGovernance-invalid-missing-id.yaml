# Missing required 'id' field — 'id' is a required property for AIGovernance
# (original intent: AISystem-invalid-enum — ai_type enum validation, not enforced by open-world JSON Schema)
name: "Bad AI Governance"
